---
# XDC Node Role Handlers

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes

- name: Restart XDC node
  ansible.builtin.systemd:
    name: xdc-node
    state: restarted
  when: restart_on_change | default(true) | bool
